// Thêm đoạn code này vào file route.ts đang trống
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: "Heatmap API is under construction" });
}